#include <stdio.h>
#include <inttypes.h>

int main() {
    unsigned int target_candidate_number = 1;
    size_t parties = 1;

    char** party_names = calloc(parties, sizeof(char*))
    unsigned int* party_votes = calloc(parties, sizeof(unsigned int))
    int* party_alignment = calloc(parties, sizeof(int))

    

    printf("whats up???\n");
    return -1;
}